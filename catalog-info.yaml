apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: gateway-backstage
  description: Spring Boot Cloud Gateway criado via Backstage com CI/CD e ArgoCD
  annotations:
    github.com/project-slug: backstage-learning-durelli/gateway-backstage
    argocd/app-name: gateway-backstage
    backstage.io/kubernetes-id: gateway-backstage
  labels:
    system: gateway-backstage
    managed-by: backstage
    type: gateway
spec:
  type: service
  lifecycle: experimental
  owner: group:default
  system: gateway-backstage
